# database-postgres

PostgreSQL database provider module for MuxCore. Implements the `DatabaseProvider` contract.

## Contract

Implements `contracts.DatabaseProvider` from `github.com/Muxcore-Media/core/pkg/contracts`.
